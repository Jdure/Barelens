/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    images: {
        domains: ["res.cloudinary.com", "source.unsplash.com"],
    },
}

module.exports = nextConfig;
